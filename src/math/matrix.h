/*
 * Internal matrix helpers. The public API in <flux/flux.h> exposes
 * inline constructors (identity, translation, scaling) and out-of-line
 * heavyweights (rotation, multiply, invert, transform_*). This header
 * re-declares the out-of-line ones for internal callers and adds nothing
 * private.
 */
#ifndef FLUX_MATH_MATRIX_H
#define FLUX_MATH_MATRIX_H

#include "flux/flux.h"

#endif /* FLUX_MATH_MATRIX_H */
